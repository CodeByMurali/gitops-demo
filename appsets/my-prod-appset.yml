apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: oneview-prod-appset
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - git:
      repoURL: https://github.com/kostis-codefresh/many-appsets-demo.git
      revision: HEAD
      directories:
      - path: apps/*/envs/prod-us
      - path: apps/*/envs/prod-eu      
  template:      
    metadata:
      # This defines the template section of an ArgoCD ApplicationSet
      # The metadata.name uses Go templating to dynamically generate application names
      # It extracts the 2nd segment (index 1) and 4th segment (index 3) from the directory path
      # For example, if path is "apps/myapp/envs/prod-us", the name becomes "myapp-prod-us"
      name: '{{index .path.segments 1}}-{{index .path.segments 3}}' 
    spec:
      # The project the application belongs to.
      project: default

      # Source of the application manifests
      source:
        repoURL: https://github.com/kostis-codefresh/many-appsets-demo.git
        targetRevision: HEAD
        path: '{{.path.path}}'
      
      # Destination cluster and namespace to deploy the application
      destination:
        server: https://kubernetes.default.svc
        # This line dynamically generates the namespace name using Go templating
        # It combines the 2nd segment (app name) and 4th segment (environment) from the directory path
        # For example, if the path is "apps/myapp/envs/prod-us", this creates namespace "myapp-prod-us"
        # The segments are zero-indexed: [0]="apps", [1]="myapp", [2]="envs", [3]="prod-us"
        namespace: '{{index .path.segments 1}}-{{index .path.segments 3}}'

      # Sync policy
      syncPolicy:
        syncOptions:
          - CreateNamespace=true  
        automated: # automated sync by default retries failed attempts 5 times with following delays between attempts ( 5s, 10s, 20s, 40s, 80s ); retry controlled using `retry` field.
          prune: true # Specifies if resources should be pruned during auto-syncing ( false by default ).
          selfHeal: true # Specifies if partial app sync should be executed when resources are changed only in target Kubernetes cluster and no git change detected ( false by default ).
      

apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: jefferies-prod
  namespace: argocd
  labels:
    client: jefferies
    environment: prod
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - git:
      repoURL: https://github.com/CodeByMurali/CDUsingArgoCD.git
      revision: sep-custom-folder
      directories:
      - path: argocd/apps/*/envs/prod      
  template:      
    metadata:
      name: 'jefferies-{{index .path.segments 2}}-{{index .path.segments 4}}'
      labels:
        client: jefferies
        service: '{{index .path.segments 2}}'
        environment: '{{index .path.segments 4}}'
    spec:
      project: jefferies-prod
      source:
        repoURL: https://github.com/CodeByMurali/CDUsingArgoCD.git
        targetRevision: sep-custom-folder
        path: '{{.path.path}}'
        kustomize:
          enableHelm: true
      destination:
        name: spoke1.us-east-1.eksctl.io
        namespace: '{{index .path.segments 4}}'
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
          - PrunePropagationPolicy=foreground
        automated:
          prune: true
          selfHeal: true

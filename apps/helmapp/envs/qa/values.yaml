# Dev-specific overrides
replicaCount: 1

service:
  type: ClusterIP  # No expensive LoadBalancer in Dev
  ports:
    http: 8080

resources:
  requests:
    cpu: 10m
    memory: 64Mi
  limits:
    cpu: 100m
    memory: 128Mi

autoscaling:
  enabled: false # Disable HPA for qa
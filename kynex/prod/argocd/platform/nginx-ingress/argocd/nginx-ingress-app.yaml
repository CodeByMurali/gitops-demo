apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nginx-ingress-kynex-prod
  namespace: argocd
  labels:
    deployment-model: kynex
    realm: prod
    service: nginx-ingress
spec:
  project: default
  source:
    repoURL: https://kubernetes.github.io/ingress-nginx
    chart: ingress-nginx
    targetRevision: 4.8.3
    helm:
      releaseName: nginx-ingress
      values: |
        controller:
          replicaCount: 3
          service:
            type: LoadBalancer
  destination:
    name: spoke1.us-east-1.eksctl.io
    namespace: ingress-nginx
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
